<h3><%= @artist.name %></h3>
<%= form_for @artist, :url => "/admin/artists/#{@artist.id}", :method => :patch do |f| %>
<%= f.label :description %><br>
<%= f.text_area :description %><br>
<%= f.submit %>
<% end %>