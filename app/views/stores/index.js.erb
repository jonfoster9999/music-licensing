<%# app/views/students/index.js.erb %>
<% js = escape_javascript(
  render(partial: 'stores/list', locals: { songs: @songs })
) %>
$("#filterrific_results").html("<%= js %>");
//add click handlers?  so that i can keep referencing the original waverunner variable
//but with new handlers